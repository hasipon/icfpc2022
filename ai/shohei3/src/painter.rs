

use crate::data::*;
use image::RgbaImage;

pub struct 
    
    score: 

pub struct PainterResult {
    rects: Vec<Rectangle>,
    commands: Vec<Command>,
    cost: i64,
    image: RgbaImage,
}

pub fn solve() -> PainterResult {
}

fn eval() -> PainterResult {
}